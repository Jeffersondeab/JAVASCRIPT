var c = 1
while (c <= 6){
    console.log(`Passo ${c}`)
    c++
} 
     
      // c++ é igual a  c = c + 1 
    /* Os operadores ++ e -- são utilizados após o nome de uma
      variável e servem para incrementar (++) ou decrementar (--)
      uma unidade da variável, ou seja, somar 1 ou subtrair
      1 do valor da variável. */

      /* O QUE EU ENTENDI FOI QUE O C++ ELE VAI 
      PROCESSAR ATÉ CHEGAR NESSE CASO NO 6.
       */

      /* ISSO É UM EXEMPLO SIMPLES DE REPETIÇÃO 
      COM WHILE.  ELE VAI SER EXECUTADO ATÉ 
      6 PASSO 1, PASSO2 ETC  */


    // É POSSÍVEL FAZER COM 'DO' EXEMPLO ABAIXO

    var c = 1
    do {
        console.log(`passo ${c}`)
        c++
    }while (c <= 6)

    // É O MESMO RESULTADO DO DE CIMA, MAS A SÓ DO WHILE É A MAIS VISTA NOS PROGRAMAS